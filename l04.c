#include <stdio.h>
void seg_consec(int *s, len_s){
	int i_atual = 0
	int atual s[i_atual];
	int tamanho = 1;
	for (int i = 1; i < len_s; i++){
		if ( s[i] == atual) {
			tamanho = s[i] - s[i_atual];
			for (int j = i; j < i_atual; j++) {
				break;
			}
		}
	}
}

int main() {

	return 0;
}
